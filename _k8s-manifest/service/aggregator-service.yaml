apiVersion: v1
kind: Service
metadata:
  name: aggregator-service
  labels:
    app: aggregator-service
spec:
  type: NodePort
  selector:
    app: aggregator-service
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080